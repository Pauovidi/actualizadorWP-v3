// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // añade aquí lo que ya tuviera tu config: images, headers, rewrites, etc.
};

export default nextConfig;
