# Production rollback checkpoint — 2026-09-04

Verified against the refreshed Vercel deployment page before code changes:

| Field | Value |
| --- | --- |
| GitHub repository | `Pauovidi/actualizadorWP-v3` |
| Production branch | `integration/actualizador-wp-automatizado` |
| Exact commit | `84aa2b6f1dcc9fa5857a09bd6685f0a45cf53b58` |
| Remote/local tag | `rollback/production-20260904-84aa2b6` |
| Vercel project | `devestial/actualizador-wp-automatizado` |
| Deployment | `dpl_NfLHiV7qnuz58ujUXW7fw8sMzECe` |
| Immutable URL | `https://actualizador-wp-automatizado-allbayomi-devestial.vercel.app` |
| Production alias | `https://actualizador-wp-automatizado.vercel.app` |
| Observed status | Ready, Production, Current |
| Production cron | Enabled, `/api/cron/billing-run`, `10 5 1 * *` UTC |

[Open the preserved deployment](https://vercel.com/devestial/actualizador-wp-automatizado/NfLHiV7qnuz58ujUXW7fw8sMzECe).

## Fast return to the existing deployment

After a future deployment, use Vercel's rollback/promote flow to select the exact
deployment above. Verify the resulting production alias and source SHA. Prefer
reusing the existing artifact to rebuilding it. With an already authenticated CLI:

```text
vercel rollback https://actualizador-wp-automatizado-allbayomi-devestial.vercel.app --scope devestial
```

Do not execute rollback as a “test” on working production. On the audit date the
deployment was already Current, so its Instant Rollback/Promote actions were
disabled; Redeploy was available. Availability of this retained artifact later
must be verified. No retention setting was changed.

Inspect Cron Jobs after rollback and restore the documented baseline schedule if
necessary. Do not assume application rollback restores scheduler state or reverses
external operations. Do not run the baseline cron manually to verify rollback:
its original dry-run implementation can update sites and write records.

## Rebuild fallback

Fetch the tag and check out the exact commit in a separate clean directory. Run
the baseline build gates and deploy it to the same Vercel project with its
existing configuration. Do not pull/export secrets. Rebuilding can be affected by
runtime deprecation, dependency availability or Vercel release policies; it is
not equivalent to restoring a retained artifact.

```text
git fetch origin tag rollback/production-20260904-84aa2b6
git switch --detach rollback/production-20260904-84aa2b6
git rev-parse HEAD
```

An offline bundle with complete history through this commit was also created:
`actualizador-production-20260904.bundle` (kept beside the audit checkout).
`git bundle verify` succeeded. SHA-256:

```text
5a06454ac6f82d5cb6463acb0717280da28d87b0aac6c9782d90623084b2392e
```

Restore it into a fresh directory using `git clone <bundle-file> <new-directory>`
and select the tag. The archive contains source/history, not a database backup,
Blob contents, environment configuration, bank mandates or WordPress backups.

## Boundaries

No production deployment, database mutation, Blob write, client email, WordPress
update, secret rotation or payment was performed during this checkpoint creation.
Git tags and bundles restore code only. A subsequent code rollback cannot undo
sent mail, issued invoices, WordPress updates or payments. Preserve operation
ledgers during recovery; never erase them to bypass duplicate protection.
